const buttonLogin = document.getElementById('button-login');

buttonLogin.addEventListener('click', () => {
  let emailPhoneValue = document.getElementById('email-phone').value;
    alert(emailPhoneValue);
});
